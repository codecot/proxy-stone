import { Box, Typography } from '@mui/material';

export default function ProxyConfig() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Proxy Configuration
      </Typography>
      <Typography>Configure your proxy server settings and behavior.</Typography>
    </Box>
  );
}
