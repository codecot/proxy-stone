# Environment for UI development (UI runs locally)
# Copy to apps/ui/.env when developing UI locally

# API endpoint (points to Docker container)
VITE_API_URL=http://localhost:4000
REACT_APP_API_URL=http://localhost:4000

# Development settings
VITE_DEV_MODE=true
VITE_LOG_LEVEL=debug 