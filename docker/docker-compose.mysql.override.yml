# MySQL database override
# Combine with any development scenario
# Example: docker compose -f docker-compose.yml -f docker-compose.ui-dev.yml -f docker-compose.mysql.override.yml up

# No additional service overrides needed for MySQL backend development
# Backend runs locally and connects to MySQL via localhost:3306
services:
  {}

  # No backend service configuration needed for backend development mode
  # Backend runs locally and connects to MySQL via localhost:3306

  # No UI configuration needed for backend development mode
  # UI can be run locally or disabled
